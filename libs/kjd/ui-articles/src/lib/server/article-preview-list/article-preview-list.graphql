#import ArticlePreview from "../../client/article-preview/article-preview.graphql"

fragment ArticlePreviewList on Query {
  articles(first: 10, orderBy: createdAt_DESC) {
    __typename
    ...ArticlePreview
  }
}
